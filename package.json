{
	"name": "hubble-md",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"check": "biome check .",
		"check:fix": "biome check . --write",
		"typecheck": "pnpm -r --if-present typecheck",
		"build": "pnpm check && pnpm typecheck && pnpm -r --if-present build",
		"bundle:desktop": "pnpm --filter @hubble.md/desktop... --if-present build && pnpm --filter @hubble.md/desktop tauri build",
		"dev:desktop": "pnpm -r --parallel --stream --filter \"./packages/**\" --if-present dev & pnpm --filter \"./apps/desktop\" tauri dev",
		"dev:sync-backend": "pnpm --filter \"./packages/sync-backend\" exec convex dev"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.4"
	}
}
